# XNAND

Minimal website, mostly just an index of the various instances of FOSS front-ends I run.

You can easily test the website locally with `python3 -m http.server`.

While HTML is inherently open source, this repo serves to make changes transparent.